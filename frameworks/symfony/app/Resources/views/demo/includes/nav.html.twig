            <div class="col-sm-3 sidebar">
                <ul class="nav nav-sidebar">
{% for filename, title in menuEntries %}
                    <li {% if filename == 'symfony/' %} class="active" {% endif %}><a href="/exp/{{ filename }}">{{ title }}</a></li>
{% endfor %}
                </ul>
            </div>
